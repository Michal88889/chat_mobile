import {
  StyleSheet
}
from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#eee',
    flex: 0,
    alignSelf: 'stretch',
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0
  },

  text: {
    color: '#000'
  }
});
